apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: axieanalytics-postgresql-pvc
  namespace: axieanalytics
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi