apiVersion: v1
kind: Service
metadata:
  name: axieanalytics-backend
  namespace: axieanalytics
spec:
  selector:
    app: axieanalytics-backend
  ports:
    - protocol: TCP
      port: 80
      name: http
      targetPort: 8080
    - protocol: TCP
      port: 443
      name: https
      targetPort: 80
  type: ClusterIP