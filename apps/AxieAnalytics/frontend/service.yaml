apiVersion: v1
kind: Service
metadata:
  name: axieanalytics-frontend
  namespace: axieanalytics
spec:
  selector:
    app: axieanalytics-frontend
  ports:
    - protocol: TCP
      port: 80
      name: http
      targetPort: 80
    - protocol: TCP
      port: 443
      name: https
      targetPort: 80
  type: ClusterIP