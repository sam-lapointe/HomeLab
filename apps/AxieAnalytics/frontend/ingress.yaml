apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: axieanalytics-ingress-frontend
  namespace: axieanalytics
  annotations:
    cert-manager.io/cluster-issuer: azuredns-issuer
    traefik.ingress.kubernetes.io/router.entrypoints: web, websecure
spec:
  ingressClassName: traefik-external
  rules:
    - host: test.axieanalytics.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: axieanalytics-frontend
                port:
                  number: 80
  tls:
    - hosts:
        - test.axieanalytics.com
      secretName: axieanalytics-com-tls